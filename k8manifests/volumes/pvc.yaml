apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: web-pvc
  namespace: webns #PVC can be namespace specific
spec:
  storageClassName: manual #match with pv
  accessModes: #match with pv
    - ReadWriteOnce 
  resources:
    requests: #requests how much storage is needed
      storage: 8Gi